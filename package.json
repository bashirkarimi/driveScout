{
  "name": "drive-scout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm run build --workspace=@drive-scout/search-widget",
    "dev": "npm run build && npm run dev --workspace=@drive-scout/search-server",
    "start": "npm run build && npm run start --workspace=@drive-scout/search-server",
    "widget:dev": "npm run dev --workspace=@drive-scout/search-widget"
  },
  "devDependencies": {
    
  }
}
