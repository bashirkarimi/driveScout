{
  "name": "drive-scout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm run build --workspace=packages/search-widget",
    "dev": "npm run build && npm run dev --workspace=apps/search-server",
    "start": "npm run build && npm run start --workspace=apps/search-server",
    "widget:dev": "npm run dev --workspace=packages/search-widget"
  },
  "devDependencies": {
    
  }
}
