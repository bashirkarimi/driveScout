{
  "version": 2,
  "buildCommand": "pnpm install && pnpm build",
  "rewrites": [
    {
      "source": "/mcp",
      "destination": "/api/mcp"
    },
    {
      "source": "/mcp/:path*",
      "destination": "/api/mcp"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "ALLOWED_ORIGIN": "https://chatgpt.com"
  },
  "functions": {
    "api/mcp.js": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
