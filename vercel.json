{
  "version": 2,
  "buildCommand": "npm run build --prefix packages/search-widget",
  "installCommand": "npm install --prefix apps/search-server && npm install --prefix packages/search-widget",
  "rewrites": [
    {
      "source": "/mcp",
      "destination": "/api/mcp"
    },
    {
      "source": "/mcp/:path*",
      "destination": "/api/mcp"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "ALLOWED_ORIGIN": "https://chatgpt.com"
  },
  "functions": {
    "api/mcp.js": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
